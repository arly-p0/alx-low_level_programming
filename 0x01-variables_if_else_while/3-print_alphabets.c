#include <stdio.h>
/**
 * main - prints alphabets lower and uppercase
 *
 * Return: always 0
 */
int main(void)
{
	int i = 97;
	int j = 65;

	while (i <= 122)
	{
		putchar(n);
	}
	while (j <= 90)
	{
		putchar(j);
	}
	putchar('\n');
	return (0);
}

#include <stdio.h>
#include <time.h>
/**
 * main - entery point
 * Return: 0 (success)
 */
int main(void)
{
	int d;
	char low;

	for (d = '0'; d <= '9'; d++)
	putchar(low);
	putchar('\n');
	return (0);
}
